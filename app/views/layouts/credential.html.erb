<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'credentials', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/credentials_nav' %>
     
    <%= render 'shared/credentials_masthead' unless logged_in?(:site_admin) %>
    
    <% login_helper %>
    
    <%= yield %>
    
    <%= render 'shared/credentials_footer' %>
    
    <%= source_helper("application") %>
  </body>
</html>
